# 面试麦开发协作规范

面试麦提供简历管理、AI 面试押题、模拟面试、评估报告及账户权益服务。本仓库使用 pnpm Monorepo 管理 Next.js 前端与 NestJS 后端，两端独立运行和部署。

本文件约束整个仓库；应用内的 `AGENTS.md` 补充该应用规则。默认用中文沟通和编写项目文档，代码标识符沿用英文。

## 1. 开始任务时

1. 查看 `git status --short`、当前分支和任务涉及的文件，识别并保留已有改动。
2. 阅读本文件，再按任务阅读[前端规范](apps/web/AGENTS.md)或[后端规范](apps/server/AGENTS.md)。跨端任务两份都读。
3. 查看对应应用的 `ai/skills/README.md`，只加载当前操作需要的技能；涉及具体模块时，再读该模块的 `Agent.md` 和实现。
4. 用 `rg` 查找现有入口、调用方及测试，明确修改前后的预期行为。不要先建一套实现，再发现仓库已有同类能力。
5. 多步骤任务简要说明改动范围和验证方式。低风险细节按现有约定推进；只有缺失信息会改变业务含义、数据影响或授权范围时才澄清。

用户当前明确要求优先于仓库习惯。代码、测试和实际配置用于确认当前行为；旧功能文档可能滞后，发现冲突应查证并修正文档，不能把过时说明当成已实现事实。应用的 `ai/Agent.md` 仅保留兼容跳转，不再维护另一套规则。

## 2. 目录与职责

| 位置 | 职责与约束 |
| --- | --- |
| `apps/web` | Next.js App Router、React 页面、Zustand 状态、HTTP/SSE 客户端 |
| `apps/server` | NestJS API、鉴权、MongoDB 数据、面试 Agent、权益与支付 |
| `docs` | 架构、迁移及跨应用开发说明 |
| `packages/*` | 已预留 workspace 匹配规则；当前尚无共享包，不要为了目录完整而创建空包 |
| 根 `package.json` / `pnpm-workspace.yaml` / `pnpm-lock.yaml` | 工作区命令、包范围、统一依赖解析 |
| `.github/workflows/ci.yml` | 安装、lint、单元测试、构建；不负责部署 |
| 原 `man-shi-mai-web/` / `man-shi-mai-server/` | 已从工作区移除；历史保留在 Git 提交图中，开发只使用 `apps/` |

前端不得直接导入后端 Service、Mongoose Schema 或服务端配置。出现真实复用需求时，可以将无框架依赖的请求、响应和事件类型提取到 `packages/contracts`，再通过 workspace 包引用；不要用跨应用相对路径绕过依赖声明。

## 3. 修改原则

- 先定位问题所属层，再修改。修复回归时对照历史正常链路，避免在错误中间态上叠加补丁。
- 每项改动应能对应当前需求；保留无关代码、注释、格式和用户修改。重构前后都要验证既有行为。
- 优先复用已有请求封装、状态库、UI 组件和服务。公共能力放在合适的共享层，业务专属逻辑留在模块内。
- 新增或修改的边界数据使用明确类型与运行时校验；不要用新增 `any`、`@ts-ignore` 或关闭 lint 规则掩盖问题。现有宽松类型配置不代表新增代码可以省略契约。
- 错误必须可定位，用户操作必须有失败状态。不得用假数据或静默成功掩盖网络、数据库和模型失败。
- 新依赖应由任务的实际需要支撑；不附带升级无关框架。只更新使用该依赖的包清单及根锁文件。
- 不强行统一两端的 TypeScript、ESLint 和测试配置；兼容性以各应用现有配置及构建结果为准。

## 4. 跨端修改要一起检查什么

修改接口时，沿以下链路检查实际调用方，不能只改一端的类型声明：

```text
页面 / 组件 → Zustand / API 封装 → HTTP 或 SSE 代理
          → Controller / DTO → Service / Schema → 返回值 → 前端状态与显示
```

- 约定路由、方法、鉴权、字段含义、可空性、状态枚举及错误返回；搜索 Store 中的直接请求，不能只检查 `src/api`。
- 普通 JSON 响应与 SSE 是两条链路：字段或事件变更同时检查生产方、代理、解析器与 UI 消费方。
- 数据结构变更说明已有记录如何读取、是否需要回填；修改 Mongoose Schema 不等于已经完成数据迁移。
- 两端同仓不代表同时上线。接口变更需要向后兼容或明确部署顺序；不要默认旧前端已经消失。
- 路由、环境变量、模块职责或使用方式变化时，同步 README、相关 `Agent.md` 或 `docs`。纯内部修复无需制造无关文档。

## 5. 环境与命令

具体版本以 [package.json](package.json) 和 [.nvmrc](.nvmrc) 为准，目前使用 Node.js 22、pnpm 10。所有以下命令从仓库根目录执行：

| 操作 | 命令 |
| --- | --- |
| 安装依赖 | `pnpm install --frozen-lockfile` |
| 同时开发前后端 | `pnpm dev` |
| 单独开发 | `pnpm dev:web` / `pnpm dev:server` |
| 检查代码 | `pnpm lint` |
| 单元测试 | `pnpm test` |
| 构建 | `pnpm build` 或 `pnpm build:web` / `pnpm build:server` |
| 完整检查 | `pnpm check` |
| 更新技能索引 / 安装提交钩子 | `pnpm skills:update` / `pnpm hooks:install` |

默认前端 8000、后端 3000；配置准备及生产启动见 [README](README.md)。只维护根 `pnpm-lock.yaml`，不要在子应用运行 npm、yarn 或 bun 安装。新增依赖使用 `pnpm --filter @man-shi-mai/web add <package>` 或对应的 server 包。

## 6. 按改动选择验证

| 改动类型 | 最低验证要求 |
| --- | --- |
| 纯文档、协作规则 | 核对路径、链接、命令和代码事实；`git diff --check`；不为文档改动重复构建或新增业务测试 |
| 前端 UI、状态或请求 | 相关单元测试（有逻辑变化时补回归用例）、前端 lint 和 build；交互变化检查对应页面 |
| 后端 Controller、Service、DTO、Schema | 相关模块测试、后端 lint 和 build；覆盖正常、拒绝和失败路径 |
| 跨端接口、SSE、鉴权或支付 | 两端相关测试和构建；验证实际请求/响应或流式行为，不能只依赖类型检查 |
| workspace、依赖、构建、共享配置 | 冻结锁文件安装与根 `pnpm check` |

先跑能复现问题的窄范围检查，再按影响扩展。测试失败要分析原因，不得仅修改断言来迎合错误实现；已存在的失败与本次引入的问题分别说明。

`pnpm test` 是单元测试，不代表真实 MongoDB、AI、OSS、支付或登录 E2E 已通过。E2E 与 `pnpm db:seed` 需专用测试数据库，准备方式见 README。不得默认连接生产数据或用真实付费请求代替 Mock。

## 7. Git、配置与交付

- 只暂存本任务文件，提交前检查 `git diff --cached`；不自动丢弃、覆盖或夹带用户已有修改。
- 提交遵循应用内 `ai/skills/git-commit/SKILL.md`：英文 Conventional Commits 前缀、中文描述，例如 `fix(interview): 修复断连后的重复消息`。
- 提交前运行 `pnpm skills:update`；已安装的钩子会更新并暂存技能索引，它不能替代测试。
- 分支、提交和推送按用户已授权范围执行；不要从其他项目复制 `dev/test/main` 晋级规则，也不要擅自强推或重写远端历史。
- `.env*`、凭据、日志及生成物不入库，示例配置只保留占位值。不要输出完整 Token、简历个人信息或支付密钥；沿用现有日志脱敏工具。
- 完成时说明改了什么、关键文件、实际验证结果，以及未验证部分及原因。区分本地通过、CI 通过、已推送和已部署，不能混为一谈。

## 8. 维护本规范

根文件只保留跨应用规则；应用细节写入各自 `AGENTS.md`，模块业务说明写入邻近 `Agent.md`，长篇方案放入 `docs`。命令、目录或接口职责改变时同步更新对应规则。不要写入临时任务进度、易过期的测试数量、未经验证的保证或其他项目专属流程。
